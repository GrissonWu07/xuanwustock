# 后端能力与服务接口清单

本文档只记录当前代码已经存在的后端边界，不再沿用旧的 `watchlist.db / candidate_pool / portfolio_stocks.db` 架构描述。

## 1. 网关入口

- 运行入口：`app/gateway.py`
- API 与路由：`app/gateway_api.py`
- 上下文装配：`app/gateway/context.py`

当前单服务提供：

- SPA 静态页面
- `/api/v1/*` 快照接口
- `/api/v1/*/actions/*` 动作接口

当前 SPA gateway 入口文件是 `app/gateway_api.py`。

当前 SPA 主路由包括：

- `/main`
- `/discover`
- `/research`
- `/portfolio`
- `/live-sim`
- `/his-replay`
- `/strategy-config`
- `/settings`

保留但不作为默认导航入口：

- `/real-monitor`

AI 盯盘后端 API 仍存在于 `/api/v1/monitor/ai`，但当前 SPA 路由清单没有挂载 `/ai-monitor`。

## 2. 主要数据库

### `data/xuanwu_stock.db`

职责：

- 统一股票池主表 `stock_universe`
- 实时量化账户和执行状态
- 策略配置

### `data/xuanwu_stock_replay.db`

职责：

- 历史回放任务和结果

### `data/selector_results/*.json`

职责：

- 发现股票和研究情报的结果缓存

## 3. 股票池相关服务

### `WatchlistService`

文件：`app/watchlist_service.py`

当前真实职责：

- 操作 `stock_universe`
- 管理 `watched` 视角
- 手工添加股票时写 `basic_info_missing`

### `CandidatePoolService`

文件：`app/quant_sim/candidate_pool_service.py`

当前真实职责：

- 对 `stock_universe.quant_enabled` 的兼容封装
- 不是独立物理候选池数据库

### `PortfolioDB / PortfolioManager`

文件：

- `app/portfolio_db.py`
- `app/portfolio_manager.py`

当前真实职责：

- 在 `stock_universe` 上管理登记持仓字段和 `registered_position_enabled`

## 4. 工作台相关接口

### 页面快照

- `GET /api/v1/workbench`

### 主要动作

- `POST /api/v1/workbench/actions/add-watchlist`
- `POST /api/v1/workbench/actions/batch-quant`
- `POST /api/v1/workbench/actions/batch-portfolio`
- `POST /api/v1/workbench/actions/delete-watchlist`
- `POST /api/v1/workbench/actions/analysis`
- `POST /api/v1/workbench/actions/analysis-batch`

说明：

- `refresh-watchlist` 仍有接口名，但后端当前直接返回“旧关注池行情刷新已停用，请使用统一数据刷新任务”。

## 5. 发现股票与研究情报

### 页面快照

- `GET /api/v1/discover`
- `GET /api/v1/research`

### 主要动作

- `POST /api/v1/discover/actions/run-strategy`
- `POST /api/v1/discover/actions/item-watchlist`
- `POST /api/v1/discover/actions/batch-watchlist`
- `POST /api/v1/discover/actions/reset-list`
- `POST /api/v1/research/actions/run-module`
- `POST /api/v1/research/actions/item-watchlist`
- `POST /api/v1/research/actions/batch-watchlist`
- `POST /api/v1/research/actions/reset-list`

### 持久化

- `selector_results/*.json`
- 写入工作台股票池时最终落到 `stock_universe`

## 6. 持仓诊断

### 页面快照

- `GET /api/v1/portfolio_v2`
- 兼容别名仍保留若干 `/api/v1/portfolio/actions/*`

### 个股详情

- `GET /api/v1/portfolio_v2/positions/{symbol}`
- `PATCH /api/v1/portfolio_v2/positions/{symbol}`

### 主要动作

- `POST /api/v1/portfolio_v2/actions/analyze`
- `POST /api/v1/portfolio_v2/actions/refresh-portfolio`
- `POST /api/v1/portfolio_v2/actions/schedule-save`
- `POST /api/v1/portfolio_v2/actions/schedule-start`
- `POST /api/v1/portfolio_v2/actions/schedule-stop`
- `POST /api/v1/portfolio_v2/actions/refresh-indicators`
- `POST /api/v1/portfolio_v2/actions/update-position`
- `POST /api/v1/portfolio_v2/actions/delete-position`

说明：

- `refresh-indicators` 仍是当前接口名，设计目标上会继续拆成更明确的数据域刷新动作。

## 7. 实时量化

### 页面快照

- `GET /api/v1/quant/live-sim`
- `GET /api/v1/quant/live-sim/signals`
- `GET /api/v1/quant/live-sim/trades`

说明：

- `GET /api/v1/quant/live-sim` 是轻量首屏快照，只聚合配置、状态、账户指标、实时量化股票、资金池和费用统计。
- 信号和成交记录不再塞进首屏快照，分别通过 `signals` 和 `trades` 分页接口读取。

### 主要动作

- `POST /api/v1/quant/live-sim/actions/save`
- `POST /api/v1/quant/live-sim/actions/start`
- `POST /api/v1/quant/live-sim/actions/stop`
- `POST /api/v1/quant/live-sim/actions/reset`
- `POST /api/v1/quant/live-sim/actions/analyze-candidate`
- `POST /api/v1/quant/live-sim/actions/delete-candidate`
- `POST /api/v1/quant/live-sim/actions/delete-position`
- `POST /api/v1/quant/live-sim/actions/bulk-quant`

### 核心服务

- `app/quant_sim/engine.py`
- `app/quant_sim/signal_center_service.py`
- `app/quant_sim/portfolio_service.py`
- `app/quant_sim/scheduler.py`

### 当前调度口径

- 默认 `interval_minutes = 10`
- quote TTL = `2` 分钟
- fresh entry 不重复远程拉
- failure cooldown 生效

## 8. 历史回放

### 页面快照

- `GET /api/v1/quant/his-replay`
- `GET /api/v1/quant/his-replay/progress`
- `GET /api/v1/quant/his-replay/capital-pool`

### 主要动作

- `POST /api/v1/quant/his-replay/actions/start`
- `POST /api/v1/quant/his-replay/actions/cancel`
- `POST /api/v1/quant/his-replay/actions/delete`

当前没有：

- `actions/continue`

### 核心服务

- `app/quant_sim/replay_service.py`
- `app/quant_sim/replay_runner.py`

### 当前回放数据边界

- 股票范围来自 `stock_universe.quant_enabled`
- 结果写入 `xuanwu_stock_replay.db`
- checkpoint 阶段只读已准备历史数据

## 9. 策略配置

### 页面数据来源

- `GET /api/v1/settings`
- `GET /api/v1/quant/live-sim`
- `GET /api/v1/strategy-profiles`
- `GET /api/v1/strategy-profiles/{profile_id}`

### 主要动作

- `POST /api/v1/strategy-profiles`
- `PUT /api/v1/strategy-profiles/{profile_id}`
- `POST /api/v1/strategy-profiles/{profile_id}/clone`
- `POST /api/v1/strategy-profiles/{profile_id}/validate`
- `POST /api/v1/strategy-profiles/{profile_id}/set-default`
- `DELETE /api/v1/strategy-profiles/{profile_id}`

### 当前策略口径

- 内置策略为 `aggressive`、`stable`、`conservative`。
- 新库默认策略是 `aggressive`。
- 三档内置策略都使用 `hybrid` 双轨融合。
- 个股执行反馈和组合防守配置随策略 profile 持久化。

## 10. 刷新与缓存

### 当前可确认已落地的行为

1. `stock_refresh_scheduler` 对 fresh runtime entry 直接命中本地。
2. `stock_refresh_scheduler` 对失败 entry 做冷却跳过。
3. `LocalMarketDataStore.fetch_latest()` 用于实时 quote TTL。
4. `LocalMarketDataStore.fetch_range()` 用于历史区间 K 线和指标。
5. 历史回放准备阶段走 local-first，并对空区间和空股票写负缓存。

### 当前仍保留的兼容点

1. `CandidatePoolService` 这个命名仍然存在。
2. `refresh-indicators` 这个动作名仍然存在。
3. 发现/研究结果仍先落 JSON，再由用户推进到股票池。

## 11. 当前不该再写进文档的旧口径

- `watchlist.db` 是主关注池数据库
- `candidate_pool` 是独立物理候选池主表
- `portfolio_stocks.db` 是当前持仓主库
- live-sim 默认 15 分钟
- his-replay 支持 continue 到 live-sim
- `/ai-monitor` 是当前 SPA 主路由
